{
  "name": "vartistic-build",
  "version": "1.0.0",
  "description": "Vartistic Studio — production build toolchain",
  "scripts": {
    "build": "node build.js",
    "prebuild": "echo '🔨 Starting Vartistic build...'"
  },
  "dependencies": {
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "javascript-obfuscator": "^4.1.1",
    "terser": "^5.31.6"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
